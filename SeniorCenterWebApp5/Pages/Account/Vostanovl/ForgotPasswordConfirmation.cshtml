@page
@model SeniorCenterWebApp.Pages.Account.Vostanovl.ForgotPasswordConfirmationModel

@{
    ViewData["Title"] = "Письмо отправлено";
}

<div class="d-flex align-items-center justify-content-center min-vh-100"
     style="background: var(--primary-3);">

    <div class="card shadow-lg rounded-4 p-4 text-center" style="max-width: 400px; width: 100%;">
        <h2 class="fw-bold text-primary mb-3">
            <i class="bi bi-envelope-check-fill"></i> Письмо отправлено
        </h2>

        <p class="text-muted">
            Если email существует, мы отправили ссылку для сброса пароля.
        </p>

        <a asp-page="/Account/Login" class="btn btn-primary rounded-pill mt-3">
            Вернуться ко входу
        </a>
    </div>
</div>