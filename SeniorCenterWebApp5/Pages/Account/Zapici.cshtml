@* @page
@model SeniorCenterWebApp.Pages.Account.ZapiciModel
@{
    ViewData["Title"] = "Занятия";
}
 *@

<!-- Подключение Bootstrap и Bootstrap Icons -->
@* <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
 *@
@* <style>
    /* Общие стили для голубых и синих тонов */
    body {
        background-color: #f8f9fa; /* светлый фон */
    }
    /* Заголовки */
    h1 {
        color: #1c41b0; /* синий цвет */
    }
    /* Фильтр */
    .filter-form {
        background-color: #e0f7fa; /* нежно-голубой фон */
        padding: 20px;
        border-radius: 15px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    /* Таблица в голубых тонах */
    .blue-table thead {
        background-color: #3399ff; /* синий */
        color: #fff;
    }

    .blue-table tbody tr:nth-child(even) {
        background-color: #e6f0ff; 
    }

    .blue-table tbody tr:nth-child(odd) {
        background-color: #f0faff; 
    }

    .blue-table tbody tr:hover {
        background-color: #b3d1ff;
    }
    /* Кнопки */
    .btn-enroll {
        background-color: #0069d9;
        color: #fff;
        transition: background-color 0.3s;
    }

        .btn-enroll:hover {
            background-color: #0056b3;
        }
</style>

<div class="container py-5">
    <!-- Заголовок и описание -->
    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold mb-3">Наши занятия</h1>
        <p class="lead text-muted">
            Добро пожаловать! Ниже представлен список доступных занятий. Выберите нужное место или просмотрите все.
        </p>
    </div>

    <!-- Фильтр по месту проведения -->
    <div class="filter-form mb-5 mx-auto" style="max-width: 600px;">
        <form method="get" class="row g-3 align-items-center">
            <div class="col-12">
                <label for="venue" class="form-label mb-1"><i class="bi bi-geo-alt-fill me-2"></i>Место проведения</label>
                <select id="venue" name="SelectedVenue" asp-for="SelectedVenue" asp-items="Model.VenueOptions" class="form-select" onchange="this.form.submit()">
                    <option value="">Все места</option>
                </select>
            </div>
        </form>
    </div> *@

    <!-- Таблица занятий -->
   @*  <div class="table-responsive mb-5">
        <table class="table blue-table align-middle mb-0">
            <thead>
                <tr>
                    <th>Название занятия</th>
                    <th>Место проведения</th>
                    <th>День проведения</th>
                    <th>Время</th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Activities != null && Model.Activities.Any())
                {
                    foreach (var activity in Model.Activities)
                    {
                        <tr>
                            <td>@activity.ActivityName</td>
                            <td>@activity.Venue</td>
                            <td>@activity.ActivityDay</td>
                            <td>@activity.Time</td>
                            <td>
                               
                                <a class="btn btn-enroll btn-sm" href="/Account/Confirmation/@activity.Id">
                                    <i class="bi bi-plus-circle me-2"></i>Записаться
                                </a>

                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center text-muted">Нет доступных занятий по выбранным условиям.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
 *@




